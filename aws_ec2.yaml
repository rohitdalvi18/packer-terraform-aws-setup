plugin: amazon.aws.aws_ec2
regions:
  - us-east-1  
keyed_groups:
  - key: tags.OS
    prefix: ""
    separator: ""
filters:
  tag:OS:
    - ubuntu
    - amazon-linux
compose:
  ansible_user: |
    (tags.OS == 'ubuntu') | ternary('ubuntu', 'ec2-user')